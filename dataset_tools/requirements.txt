kaggle>=1.6
nibabel>=5.0
opencv-python-headless>=4.8
pillow>=10.0
numpy>=1.24
tqdm>=4.66

# NOT pip-installable — needed to extract the CardiacUDC / EchoCP spanned zips:
#   7-Zip  ->  conda install -c conda-forge p7zip   (no sudo; provides `7za`)
#          or  sudo apt-get install -y p7zip-full   (provides `7z`)
